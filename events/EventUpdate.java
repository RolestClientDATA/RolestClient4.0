package minecraft.rolest.events;

import lombok.AllArgsConstructor;
import lombok.Data;
import minecraft.rolest.utils.player.MoveUtils;
import minecraft.rolest.modules.api.ModuleManager;
import minecraft.rolest.Rol;

@Data
@AllArgsConstructor
public class EventUpdate {

    // Добавляем метод для обработки AirJump
   // public static void handleAirJumpMotion() {
      //  if (Rol.getInstance().getModuleManager().getAirJump().isState()) {
      //      MoveUtils.handleAirJumpMotion();
      //  }
  //  }
}